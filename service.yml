name: druid-operator
lang: go
lang_version: "1.25.7"
git:
  enable: true
github:
  enable: true
  repo_name: confluentinc/druid-operator
semaphore:
  enable: true
  pipeline_enable: false
  triggers:
    - branches
    - pull_requests
  branches:
    - master
    - cc-druid-operator
  managed_sections:
    - version
    - name
    - agent
    - global_job_config
